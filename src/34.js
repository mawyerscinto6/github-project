// Example of an unformatted string or variable assignment.
const user = "John Doe";

// Randomly select an integer between 1 and 100 (inclusive).
let randomNumber = Math.floor(Math.random() * 100) + 1;

// Example of a simple math expression.
let result = 5 * 3 - 2;
console.log(result);
